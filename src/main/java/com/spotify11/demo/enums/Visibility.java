package com.spotify11.demo.enums;

public enum Visibility {
    PUBLIC,       // visible to everyone
    PRIVATE,      // only owner (and admins)
    UNLISTED      // accessible via direct link (optional)
}